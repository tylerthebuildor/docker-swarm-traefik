debug = false
checkNewVersion = true
logLevel = "ERROR"
defaultEntryPoints = ["http"]
# defaultEntryPoints = ["http", "https"]

[entryPoints]
  [entryPoints.http]
  address = ":80"
  #   [entryPoints.http.redirect]
  #   entryPoint = "https"
  # [entryPoints.https]
  # address = ":443"
  # [entryPoints.https.tls]
    # [[entryPoints.https.tls.certificates]]
    # CertFile = "/etc/traefik/quangosite2.quangostaging.com.cert"
    # KeyFile = "/etc/traefik/quangosite2.quangostaging.com.key"

# [retry]

# [docker]
# endpoint = "unix:///var/run/docker.sock"
# domain = "traefik"
# watch = true
# exposedbydefault = false
# swarmmode = true

# [acme]
# email = "webmaster@quangoinc.com"
# storage = "acme.json"
# entryPoint = "https"
# onDemand = true
# OnHostRule = true
# acmeLogging = true

# defaultEntryPoints = ["http"]

# [entryPoints]
#  [entryPoints.http]
#  address = ":80"
# compress = false
#   [entryPoints.http.redirect]
#      entryPoint = "https"
#  [entryPoints.https]
#  address = ":443"
#    [entryPoints.https.tls]
#      [[entryPoints.https.tls.certificates]]
#      CertFile = "/etc/traefik/domain.com.cert"
#      KeyFile = "/etc/traefik/domain.com.key"

#[acme]
#email = jean-michel@domain.com"
#storage = "/etc/traefik/acme.json"
#entryPoint = "https"
#onDemand = true
#OnHostRule = true
#acmeLogging = true